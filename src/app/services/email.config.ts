// Email configuration used by EmailService (Formspree-style).
// If you are using EmailJS directly in components, this file simply
// satisfies the TypeScript import so the build does not fail.

export const emailConfig = {
  // Form backend endpoint (e.g., Formspree). Leave empty if not used.
  endpoint: '',
  // Default recipient; can be referenced by backends or automations.
  to: 'contato@funpefer.org'
};